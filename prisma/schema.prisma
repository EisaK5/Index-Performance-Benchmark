generator client {
  provider = "prisma-client"
  output   = "./generated"
}

datasource db {
    provider = "postgresql"
}

model Stock {
  id Int @id @default(autoincrement())
  stockName String
  date DateTime
  open Float
  high Float
  low Float
  close Float
  volume Float
  openInt Float
}
